#  Copie este arquivo para .env.local e preencha os valores.
# URL base da API principal (sem barra no final)
VITE_API_BASE_URL=http://localhost:8080

# URL base da API de Chat/WhatsApp (sem barra no final)
VITE_CHAT_API_URL=http://localhost:8000

# URL base da API geográfica em FastAPI — vulnerabilidade social e demanda
# odontológica (sem barra no final). Serve /api/v1/vulnerabilidade/*.
# Suba com: uvicorn app.main:app --reload  (porta 8000 por padrão)
VITE_GEO_API_URL=http://localhost:8000
